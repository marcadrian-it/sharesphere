query Post($id: Int!) {
  post(id: $id) {
    id
    title
    createdAt
    updatedAt
    points
    text
    imageUrl
    voteStatus
    comments {
      id
      text
      createdAt
      updatedAt
      points
      voteStatus
      author {
        id
        username
      }
    }
    author {
      id
      username
    }
  }
}
